#include<iostream>
using namespace std;
int n,a[10];
static int fs;
void sum(int *s,int l,int m,int h)
{ int i,j,n1,n2,hs,k=1;
  cout<<"\n"<<l<<" "<<m<<" "<<h;
  n1=m-l+1;
  n2=h-m;
  int s1=0,s2=0,s3=0;
  for(i=0;i<n1;i++)
  { s1=s1+s[l+i];}
  for(j=0;j<n2;j++)
  { s2=s2+s[m+j+1];}
  s3=s1+s2;
  hs=(s1>s2)?((s1>s3)?s1:s3):((s2>s3)?s2:s3);
  fs=fs+hs;
  cout<<fs<<" "<<s1<<" "<<s2<<" "<<s3<<" "<<hs;
  }
void max(int *s,int l,int h)
{ if(l<h)
  { int m=((l+h)/2);
    max(s,l,m);
    max(s,m+1,h);
    sum(s,l,m,h);}}
int main()
{ cout<<"\nEnter the no' of elements: ";
  cin>>n;
  cout<<"\nEnter the elements: ";
  for(int i=1;i<=n;i++)
  { cin>>a[i];}
  max(a,1,n);
  cout<<"\nfs: "<<fs;
  return 0;}
